Fix the CLUB DETAILS MENU cart and quantity interaction system to make it fully usable, clear, and production-ready.

---

## 🍹 1. CLUB MENU DRINKS / BOTTLE QUANTITY SYSTEM (MISSING FEATURE)

### Current Issues:

* No quantity selector for drinks/bottles
* Only “+” icon exists
* No way to increase/decrease quantity properly

### Required Fix:

Add full quantity control system for ALL club items:

### Must include:

* ➖ Decrease button
* 🔢 Quantity display (e.g., 1, 2, 3…)
* ➕ Increase button

### Rules:

* Users must be able to select quantity BEFORE adding to cart
* Quantity must be clearly visible on UI

---

## 🛒 2. ADD TO CART FEEDBACK (CRITICAL UX ISSUE)

### Current Problem:

* User clicks “+” but does NOT understand if item is added or not
* No confirmation or visual feedback

### Required Fix:

Add clear visual + system feedback:

### When item is added:

* Show one of the following:

  * ✔ “Added” badge on item card
  * OR small tick icon on product
  * OR toast message: “Added to cart”
* Cart icon should update in real-time (badge count increase)

---

## 🧠 3. CART STATE CLARITY (VERY IMPORTANT)

### Fix:

* Ensure cart updates instantly when + is clicked
* Show:

  * Item count in cart icon (top bar/bottom nav)
  * Updated total quantity per item

### Rules:

* No silent add-to-cart actions allowed
* Every action must have visible confirmation

---

## 🍾 4. CLUB MENU ITEM BEHAVIOR STANDARDIZATION

Each drink/bottle item must behave like:

### Flow:

1. User sees item
2. Selects quantity (± buttons)
3. Clicks “Add” OR “+”
4. System confirms addition visually
5. Cart updates instantly

---

## ⚠️ 5. UI/UX REQUIREMENTS

* Quantity selector must be visible on every club item
* “+” alone is NOT enough anymore
* Add-to-cart must have feedback animation or badge
* Cart icon must always reflect real-time state

---

## ✅ FINAL RESULT EXPECTED

* Full quantity control system for drinks/bottles
* Clear visible confirmation when item is added
* No confusion for user about cart state
* Professional-level restaurant/club ordering UX
