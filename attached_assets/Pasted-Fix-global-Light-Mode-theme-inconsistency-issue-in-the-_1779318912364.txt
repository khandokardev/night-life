Fix global **Light Mode theme inconsistency issue** in the entire application.

## 🎨 LIGHT MODE THEME BUG (CRITICAL UI ISSUE)

### Problem:

Even when Light Mode is enabled, many elements are still using **Gold / Dark Mode styling**, including:

* Explore button
* Settings icon
* Profile icon
* “See what’s new”
* Reserve table
* Shop now
* Ratings
* Add card
* Size / Price labels
* #Trending tags
* Upcoming events
* Map direction buttons
* Menu / Directions
* QR code icon
* View bookings
* Back / Home buttons
* Add calendar
* Booking confirmation icon
* VIP upgrade / membership button

---

## ❌ ROOT CAUSE

* Dark mode colors are leaking into Light mode
* Hardcoded gold/accent colors are used instead of theme variables
* No proper theme separation between Light and Dark mode
* Icons and text are not respecting theme context

---

## ✅ REQUIRED FIX (VERY IMPORTANT)

### 1. Central Theme System Fix

Create/repair proper theme structure:

* lightTheme (STRICT LIGHT MODE COLORS ONLY)
* darkTheme (current dark/gold style stays here)

👉 Light mode MUST NOT use:

* Gold accents
* Dark highlights
* Amber/yellow UI tones
* Dark mode icon colors

---

### 2. LIGHT MODE COLOR RULES

In Light Mode:

* Primary color → neutral / brand color (NOT gold)
* Text → dark gray / black
* Icons → dark / neutral (NOT gold)
* Buttons → light background with proper contrast
* Highlights → subtle, not neon or gold
* Cards → white / soft gray
* Borders → light gray

🚫 STRICTLY REMOVE GOLD COLOR FROM LIGHT MODE

---

### 3. COMPONENT FIX REQUIRED

Fix ALL these components:

* Bottom navigation icons
* Header icons
* Buttons (all types)
* Tags (#trending, categories)
* Labels (price, size, rating)
* Action buttons (book, reserve, add, checkout)
* Event cards
* Product cards
* Booking UI
* QR code screen
* Navigation buttons (back/home)

Each must respect theme context dynamically.

---

### 4. ICON THEME FIX

* Icons must NOT stay gold in light mode
* Use:

  * black / dark gray in light mode
  * gold only in dark mode (if intended design)

---

### 5. STATE MANAGEMENT REQUIREMENT

Ensure:

* Theme is globally applied
* No component overrides theme locally
* No inline hardcoded color styles

---

## 🧪 FINAL RESULT EXPECTED

After fix:

* Light mode = clean, modern, neutral UI
* Dark mode = gold accent UI (unchanged)
* No mixed styling anywhere
* Consistent professional design across entire app
