🚨 CRITICAL ISSUE: SIGNUP VALIDATION IS COMPLETELY BROKEN

The signup form is currently accepting invalid data and bypassing all validation rules.

This is a serious security and data integrity issue.

---

# 🔴 CURRENT PROBLEMS

## 1. EMAIL VALIDATION FAILED

* Any text is being accepted as email
* Invalid email formats are allowed
* No proper email regex validation

---

## 2. PASSWORD VALIDATION FAILED

* Weak passwords are allowed
* No minimum length enforcement
* No complexity rules (letters/numbers/symbols)
* Empty or short passwords are accepted

---

## 3. FULL NAME VALIDATION FAILED

* Any random text is accepted
* No validation for proper name format
* Empty or invalid names are allowed

---

## 4. PHONE NUMBER VALIDATION FAILED

* Any text is being accepted as phone number
* No numeric validation
* No length or country format validation

---

## 5. TERMS / "I ACCEPT" CHECKBOX BUG

* "I Accept Terms" checkbox is auto-ticked by default
* Users are NOT required to manually accept terms
* This must NEVER be pre-checked

---

# ❌ REQUIRED FIX (STRICT RULES)

## 1. EMAIL VALIDATION

* Must enforce proper email format
* Reject invalid emails
* Example valid: [user@example.com](mailto:user@example.com)
* Example invalid: "abc", "test@", "123"

---

## 2. PASSWORD VALIDATION

Must enforce:

* Minimum 8 characters
* Must include letters and numbers
* Reject weak passwords
* Reject empty passwords

---

## 3. FULL NAME VALIDATION

* Only allow valid alphabetic names
* Reject empty or numeric-only inputs
* Prevent random symbols or junk text

---

## 4. PHONE VALIDATION

* Only allow numeric input
* Reject letters and symbols
* Enforce valid length (based on system requirement)

---

## 5. TERMS & CONDITIONS CHECKBOX FIX

* Checkbox MUST be unchecked by default
* User MUST manually tick it
* Signup MUST FAIL if not checked

---

# 🔐 SIGNUP SECURITY RULE

Account creation MUST only succeed when ALL conditions are valid:

* Valid email
* Strong password
* Valid name
* Valid phone number
* Terms accepted manually

If ANY condition fails:
→ Block signup
→ Show proper error message
→ Do NOT create account

---

# 🎯 FINAL ACCEPTANCE CRITERIA

Signup is ONLY correct if:

* Invalid email is rejected
* Weak password is rejected
* Invalid phone is rejected
* Invalid name is rejected
* Terms checkbox is NOT auto-selected
* Account cannot be created with any invalid input

---

# ⚠️ IMPORTANT

Fix only validation + logic.

Do NOT change UI design or layout.
