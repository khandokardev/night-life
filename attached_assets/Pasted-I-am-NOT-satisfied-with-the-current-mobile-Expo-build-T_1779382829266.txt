I am NOT satisfied with the current mobile (Expo) build.

The current version has MAJOR issues:

* UI does NOT match original web design
* Splash screen images/backgrounds are incorrect
* Menu layouts are broken or misaligned
* Shopping UI is incorrect
* Product layout is incorrect
* Login / signup screens are not matching original design
* Icons are incorrect or inconsistent
* Overall design system is completely different from original

---

# 🔴 CRITICAL INSTRUCTION (NON-NEGOTIABLE)

You MUST RESTORE the application to EXACT ORIGINAL WEB DESIGN (1:1 clone).

This is NOT a redesign task.
This is NOT an improvement task.
This is NOT a redesign for mobile UX.

This is a STRICT REPLICATION TASK.

---

# 📌 REQUIREMENT

## 1. FULL DESIGN RESTORATION

Restore ALL original web UI exactly:

* Splash screen (same image, same layout, same positioning)
* Login / Signup screens (exact design)
* Home screen layout (exact structure)
* Shopping section (exact product grid/cards/layout)
* Product detail page (exact layout)
* Cart system UI (exact design)
* Checkout page (exact structure)
* Booking system (tables, tours, club booking exact UI)
* Profile page (exact design)
* Settings page (exact layout + icons)
* VIP membership page (exact tier UI)
* All menus (dining, club, tours) exact UI match

---

## 2. ASSET RESTORATION

* Use EXACT original images only
* Restore all background images
* Restore splash screen assets
* Restore icons (no replacement or redesign)
* Restore all banners and feature images

NO NEW ASSETS ARE ALLOWED.

---

## 3. DESIGN SYSTEM LOCK

* Colors must match original web design exactly
* Typography must match web version
* Spacing and padding must match web layout
* Card styles must match web design
* Buttons must match original UI style
* Shadows, borders, and effects must match web

NO design reinterpretation is allowed.

---

## 4. FUNCTIONALITY PRESERVATION

ALL existing features must remain:

* Cart add/remove
* Checkout system
* Payment flow (Stripe placeholder allowed for now)
* Login system
* OTP system (mock allowed for now)
* Notifications system
* Save / share options
* Map / direction system
* Booking system (modify/cancel included)
* Account deletion
* Reviews system
* Wishlist / favorites

Nothing can be removed.

---

## 5. NAVIGATION & FLOW

* Navigation must match web version exactly
* Back button must behave exactly like web
* No route replacement bugs
* No missing screens
* No broken links between pages

---

## 6. CRITICAL FIX REQUIREMENTS

Fix ALL of the following:

* Broken layouts in shopping
* Misaligned product grids
* Incorrect splash screen rendering
* Wrong menu designs
* Login/signup UI mismatch
* Icon inconsistency
* Theme mismatch (light mode must be correct)
* Gold color leakage issue (must match web theme only)

---

## 7. FINAL ACCEPTANCE RULE

The app is ONLY acceptable if:

* Every screen is visually identical to web version
* No missing UI element exists
* No design deviation exists
* No missing functionality exists
* No broken layout exists
* No incorrect images/icons exist

If ANY mismatch is found → fix it before completion.

---

# 🧠 IMPORTANT

Treat the current web version as the SINGLE SOURCE OF TRUTH.

Do NOT redesign.
Do NOT optimize.
Do NOT modernize.

ONLY replicate exactly.
