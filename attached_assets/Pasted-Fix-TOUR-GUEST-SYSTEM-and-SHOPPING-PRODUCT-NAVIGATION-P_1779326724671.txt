Fix TOUR GUEST SYSTEM and SHOPPING PRODUCT NAVIGATION / PRODUCT DETAIL BUG properly.

---

## 👥 1. TOURS → GUEST SYSTEM IMPROVEMENT (IMPORTANT FEATURE)

### Add Professional Guest Selection:

Inside every Tour booking, add:

### 👤 Guest Breakdown:

* 👨 Adults (18+)

  * * / − selector
* 🧒 Children (under 18)

  * * / − selector

---

### Requirements:

* Adults minimum = 1 (mandatory)
* Children can be 0 or more
* Total guests = Adults + Children
* Show live summary:

  * “2 Adults, 1 Child”

---

### UX REQUIREMENTS:

* Clean stepper UI (+ / − buttons)
* Prevent negative values
* Validate before checkout
* Store guest breakdown in booking state

---

## 🛍️ 2. SHOPPING PRODUCT ONLY OPENING “HOODIE” ISSUE (CRITICAL BUG)

### Current Problem:

* Only ONE product (hoodie) opens when clicking any product
* All products incorrectly redirect to same product detail

---

## ❌ ROOT CAUSE (LIKELY)

* Hardcoded product ID in navigation
* Incorrect mapping of product click handler
* Reused static product data instead of dynamic ID
* Missing dynamic routing parameter

---

## ✅ REQUIRED FIX

### 1. Dynamic Product Routing (MANDATORY)

Each product click MUST pass its unique ID:

* productId
* productSlug (if used)
* category (optional)

---

### 2. Fix Product Click Handler

* Remove any hardcoded:

  * “hoodie”
  * default product object
* Ensure each product opens its own data

---

### 3. Correct Product Detail Fetching

* Product page must:

  * Read ID from route
  * Fetch correct product from database/state
  * Render correct images, price, description

---

### 4. Fix Product List Mapping

* Ensure product list uses:

  * map(product => renderProductCard(product))
* NOT static repeated component

---

## 🧠 RESULT EXPECTED

### SHOPPING:

* Each product opens correctly (no hoodie bug)
* No duplicated product detail
* Clean dynamic routing

### TOURS:

* Adults + Children selection fully functional
* Professional guest breakdown system
* Proper booking validation

---

## ⚠️ FINAL REQUIREMENT

* No hardcoded product IDs anywhere
* No static navigation targets
* Fully dynamic data-driven system
* Production-level UX consistency