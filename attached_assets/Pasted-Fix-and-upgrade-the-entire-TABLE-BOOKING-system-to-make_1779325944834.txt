Fix and upgrade the entire TABLE BOOKING system to make it fully professional, consistent, and editable across all steps (booking → checkout → edit → re-book).

---

## 🪑 1. TABLE BOOKING FORM (MISSING CORE FIELDS)

### Add these required fields to ALL table bookings:

* 📅 Date Picker (select date)
* ⏰ Time Picker (select time)
* 👥 Guests Counter (number of guests)
* 🪑 Table Selection (available tables list or dropdown)

### Requirements:

* All fields must be mandatory
* Must validate before proceeding
* Show proper UI controls:

  * Calendar for date
  * Time selector for time
  * Stepper (+ / -) for guests
  * Table selection cards or dropdown

---

## 🧾 2. CHECKOUT → EDIT BOOKING FLOW (CRITICAL)

### Current Issue:

* After going to checkout, user cannot properly edit booking details

### Fix Required:

When user clicks **EDIT** in checkout:

### MUST RE-OPEN FULL BOOKING FORM:

* Date selection
* Time selection
* Guests count
* Table selection

### Rules:

* Editing must NOT just open a partial view
* It must restore full booking screen with previous values pre-filled
* After editing → user returns to updated checkout summary

---

## 🔁 3. BOOKING STATE PERSISTENCE (IMPORTANT)

### Fix:

* Booking data must persist across screens:

  * Booking page
  * Checkout page
  * Edit page

### Ensure:

* No data loss on navigation
* State stored globally (context/store/backend)

---

## 🪑 4. TABLE SELECTION SYSTEM (NEW FEATURE FIX)

### Requirements:

* Show available tables (example: Table 1, Table 2, VIP Table, etc.)
* Each table must:

  * Be selectable
  * Show availability status
* Prevent double booking of same table/time slot

---

## ⚠️ 5. VALIDATION RULES

Before proceeding to checkout:

* Date must be selected
* Time must be selected
* Guests must be > 0
* Table must be selected

If missing:

* Show error message:

  * “Please select date, time, guests and table to continue”

---

## 🔄 6. EDIT FLOW RULE (VERY IMPORTANT)

### When user clicks EDIT:

* Open full booking form again
* Pre-fill all previous data:

  * Date
  * Time
  * Guests
  * Table
* Allow modification
* Save updates back to checkout instantly

---

## 📊 7. UX REQUIREMENTS

* Clean professional booking UI

* Step-by-step flow:

  1. Select booking details
  2. Choose table
  3. Checkout summary
  4. Edit if needed
  5. Confirm booking

* No skipping required fields

* No broken navigation

* No data reset

---

## ✅ FINAL RESULT EXPECTED

* Fully functional table booking system
* Editable at checkout with full restoration
* Professional UI with proper selectors
* No missing fields or broken booking flow
