Fix all remaining shopping, cart, product, booking, and UI consistency issues. This is a critical production stabilization update.

---

## 🛒 1. FEATURED PRODUCT → SIZE SELECT + ADD TO CART BUG (CRITICAL)

### Current Issues:

* Users can add product to cart WITHOUT selecting size
* Size selection is not enforced
* Size selector sometimes does nothing
* Add to cart works incorrectly without validation

### Required Fix:

* Size selection MUST be mandatory for size-based products (T-shirt, cap, clothing, etc.)
* Correct flow:

  1. User opens product OR selects size
  2. Size must be selected first (if required)
  3. ONLY then “Add to Cart” becomes active

### Rules:

* Do NOT allow direct add-to-cart without required size selection
* Disable + button until valid size is selected
* Show validation message: “Please select a size”

---

## 🛍️ 2. SHOPPING ADD TO CART NOT WORKING PROPERLY

### Issues:

* Add to cart button sometimes not responding
* State not updating correctly

### Fix:

* Fix cart state management globally
* Ensure:

  * onClick handlers properly wired
  * product ID and variant (size) included
  * cart updates instantly and persistently

---

## 👕 3. BEST SELLER / T-SHIRT / CAP SIZE SYSTEM BROKEN

### Issues:

* Products requiring size have no proper selection flow
* Direct + adds item without validation

### Fix:

* If product requires size:

  * Show size selector BEFORE enabling add to cart
  * Prevent opening cart addition without selection

---

## ❤️ 4. “YOU MAY ALSO LIKE” PRODUCTS NOT CLICKABLE

### Issue:

* Suggested products are not opening

### Fix:

* Add click navigation to each item
* Ensure:

  * Opens product detail page
  * Loads correct product data
* Fix missing onClick handlers

---

## 🍽️ 5. “BOOK A MENU” ADD (+) BUTTON NOT WORKING

### Issue:

* Drink/food menu items (inside booking menu) cannot be added to cart

### Fix:

* Connect + button properly to cart system
* Ensure:

  * Item is added with correct ID
  * Quantity updates properly
* Fix broken event binding in menu section

---

## 💳 6. “BOOK A TABLE → PROCEED TO PAYMENT” BUTTON POSITION BUG

### Issue:

* Button is visually misplaced (too high / incorrect alignment)

### Fix:

* Reposition button properly:

  * Must stay fixed at bottom of container OR correct section spacing
* Ensure consistent placement across all screens
* Do NOT overlap or float incorrectly

---

## 🎨 7. DINING MENU STILL SHOWING GOLD COLOR IN LIGHT MODE

### Issue:

* Service price and icons still gold in light mode

### Fix:

* Remove ALL gold color leakage in light mode
* Apply proper theme system:

  * Light mode → neutral colors (black/gray/primary)
  * Gold only for VIP/premium badges (if intended)

---

## ⚠️ FINAL SYSTEM RULES (IMPORTANT)

* Strictly enforce product variant validation (size required items)
* Fix all broken cart interactions
* Ensure consistent add-to-cart behavior across:

  * Shopping
  * Featured products
  * Menu booking
* Fix all UI alignment issues
* Remove unintended gold styling in light mode completely
* Ensure all clickable UI elements actually respond

---

## ✅ RESULT EXPECTED

* No product can be added without required selection (size)
* All cart actions work consistently
* All product lists are clickable and functional
* Booking UI is properly aligned
* Light mode is clean with no gold leakage
